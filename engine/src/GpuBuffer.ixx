// clang-format off
module;
#include <vk_mem_alloc.h>
export module mobula.engine.gpu:GpuBuffer;
// clang-format on

namespace mobula {
  export class GpuBuffer {
  public:
    //explicit GpuBuffer(VmaAllocator allocator, )
  };
}